script(src='/javascripts/common.js')

div.panel.panel-default

	if session && session.user
		div.panel-heading My Account
		div.panel-body 
			div.inner
				div
					a(href="/users/home?userName=#{session.user.userName}").user-avatar
						img(src="#{session.user.userAvatar}")
					span.user-name
						a(href="/users/home?userName=#{session.user.userName}") #{session.user.userName}
			.row
				.col-md-5
					a(class="generate" href="/topic/add").btn.btn-primary Add Topic
				.col-md-5
					form#getUserInputs
						a(class="generate" href="/topic/generate").btn.btn-primary Generate Topics
			

	else
		div.panel-heading Wellcome to TechAlchemy
		div.panel-body 
			div.inner
				p Idea Generation Website - TechAlchemy
//- https://www.kaggle.com/airback/match-outcome-prediction-in-football/notebook